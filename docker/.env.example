# Copiar como .env y ajustar. El .env real no se versiona.
# Nombre visible del sistema (neutro por defecto, personalizable por empresa)
APP_NAME=Monitoreo de Temperatura Industrial
# Puerto expuesto en el host (la franja 8080-8161 la usa la granja securapp)
PORT=8500
